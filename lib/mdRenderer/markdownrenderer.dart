import 'package:flutter/material.dart';

class MarkdownRenderer extends StatelessWidget {
  const MarkdownRenderer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Hallo Welt");
  }
}
